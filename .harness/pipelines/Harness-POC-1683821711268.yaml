pipeline:
  identifier: Build_Harness_POC_1683821713869
  name: Build Harness-POC
  orgIdentifier: default
  projectIdentifier: POC
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github_OAuth_1683817174712
        repoName: felipetarran/Harness-POC
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: dockerbuild
                  name: docker_build
                  spec:
                    connectorRef: AWS_REAM
                    image: 883922500875.dkr.ecr.us-east-1.amazonaws.com/hrnspoc
                  timeout: ""
                  type: Plugin
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
    - stage:
        name: deploy k8s
        identifier: deploy_k8s
        description: ""
        type: Deployment
        spec:
          deploymentType: NativeHelm
          service:
            serviceRef: HarnessPoc
          execution:
            steps:
              - step:
                  name: Helm Deployment
                  identifier: helmDeployment
                  type: HelmDeploy
                  timeout: 10m
                  spec:
                    skipDryRun: false
                    ignoreReleaseHistFailStatus: false
                    delegateSelectors:
                      - helm-delegate
                  failureStrategies: []
            rollbackSteps:
              - step:
                  name: Helm Rollback
                  identifier: helmRollback
                  type: HelmRollback
                  timeout: 10m
                  spec: {}
          environment:
            environmentRef: HarnessPOC
            deployToAll: false
            infrastructureDefinitions:
              - identifier: kubernetesream
        tags: {}
        failureStrategies:
          - onFailure:
              errors:
                - AllErrors
              action:
                type: StageRollback
